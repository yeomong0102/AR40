VS에서 Git 변경사항에서 로그는 꼭 쓰고 커밋해야만 함

.git폴더를 지우는 순간 Github와 연결이 끊긴다
시키는것만 하자

내가 올린 파일의 역사를 확인하고 싶으면

상단메뉴 > git > 분기관리로 가면 된다

// ide로 식은땀을 흘리고 싶지 않으면 2가지만 있다
// 남의 프로젝트는 보기만

// 언제나 git 페치
// 언제나 git 풀
을 해서 모든 수신을 적용

// git push를 해야한다

// 불안하다면 1가지 방법이 있음
// 모두 커밋 버튼 오른쪽 화살표를 눌러보면 
// 모두 스태시 및 스테이징된 상태로 유지 항목이 있음
// 이걸 누르면 됨
// 충돌날만한 내용들을 제외하고 올린다는 느낌
// 누르면 다른 곳으로 수정사항을 보내둠 (분리된 세계)

// 그 상태로 git 페치 git풀 다해서 최신상태의 프로젝트로 만들고 f5를 눌러서 실행까지 해본다음
// 다시 꺼내서 써보면 됨
// 스테시에서 팝으로 꺼냄 꺼낸다
// 이때 충돌이 생길수도 있는데 해결시간은 다음시간